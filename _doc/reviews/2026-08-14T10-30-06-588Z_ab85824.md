# Revue IA — 2026-08-14T10:30:06.588Z

- Modèle : moonshotai/kimi-k3
- Commit parent : ab85824 (ce fichier fait partie du commit en cours de création, juste après ce parent)
- Fichiers revus : deploy/README.md, deploy/seed-cities.sh, web/email/brevo.ts, web/email/brevo_test.ts, web/email/layout.ts, web/email/layout_test.ts, web/routes/index.tsx, web/routes/inviter.tsx, web/static/llms.txt, web/utils/seo.ts
- Verdict : OK

---
## web/routes/inviter.tsx

- **Bug (certain, visible utilisateur)** : dans `loadInviterCore`, la concaténation du message WhatsApp perd l'espace/la ponctuation entre les deux segments :
  ```ts
  `(${user.street.city.name}) : partage, coup de main, bons plans` +
  `Rejoins-moi ici : ${joinUrl}`;
  ```
  produit « …bons plansRejoins-moi ici : https://… ». Il manque un séparateur (« — » ou « . ») en fin de l'avant-dernier segment. Le lien reste fonctionnel, mais le message partagé aux voisins est dégradé.
- Typo dans le flyer imprimé : « reccommandations » → « recommandations ».

## web/email/brevo.ts

- La phrase « Partage, coup de main, entraide et bons plans entre voisins. » du corps de `buildInviteEmail` est désormais **strictement identique** au nouveau pied de page de `renderEmailLayout` (layout.ts). L'e-mail d'invitation affiche donc la même phrase deux fois (corps + footer). Suggérer de la retirer du corps de l'invitation, ou de différencier les deux formulations.

## web/email/brevo_test.ts

- Nom de test obsolète : « les trois e-mails portent l'en-tête commun » alors que le tableau en contient sept (pré-existant, mais le diff retouche ce test — en profiter pour renommer, ex. « tous les e-mails transactionnels… »). À noter aussi que l'assertion vérifie désormais le **pied de page** commun, pas l'en-tête.

## deploy/seed-cities.sh, deploy/README.md, web/email/layout.ts, web/email/layout_test.ts, web/routes/index.tsx, web/static/llms.txt, web/utils/seo.ts

RAS.

VERDICT: OK
