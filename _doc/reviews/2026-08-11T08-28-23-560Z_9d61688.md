# Revue IA — 2026-08-11T08:28:23.560Z

- Modèle : moonshotai/kimi-k3
- Commit parent : 9d61688 (ce fichier fait partie du commit en cours de création, juste après ce parent)
- Fichiers revus : AGENTS.md, _doc/backlog.md, web/assets/common.css, web/assets/pages/connexion.css, web/assets/pages/fil.css, web/assets/pages/home.css, web/assets/pages/inviter.css, web/assets/pages/messages.css, web/client.ts, web/deno.json, web/email/brevo.ts, web/email/brevo_test.ts, web/email/layout.ts, web/email/layout_test.ts, web/routes/connexion.tsx, web/routes/fil.tsx, web/routes/index.tsx, web/routes/inviter.tsx, web/routes/messages.tsx
- Verdict : OK

---
Revue du diff (refonte CSS « un fichier commun + un par page », habillage des e-mails, pictos message dans le fil, redesign /messages).

**web/assets/common.css**
- `.street-status__title` (et `.compose-post__title` dans `fil.css`) codent en dur `font-family: "Newsreader", Georgia, serif` alors que le token `--font-display` existe et que Newsreader n'est volontairement pas chargée (commentaire RWEB0032 juste au-dessus). Utiliser `var(--font-display)` pour cohérence — préexistant, mais ce refactor est l'occasion de le nettoyer.
- `.fil-list__empty` est maintenant partagée entre /fil et /messages mais garde un préfixe `fil-` ; un nom neutre (`.empty-state`…) serait plus clair. Mineur.
- Les classes `.site-nav*` de l'ancien `styles.css` ont été supprimées : vérifier qu'elles n'étaient plus référencées nulle part (`Header.tsx` est hors du diff). A priori du code mort, mais à confirmer visuellement.

**web/assets/pages/home.css**
- Nommage : `home.css` pour la route `index.tsx`, alors qu'AGENTS.md documente la convention `assets/pages/<route>.css` (donc `index.css`). Harmoniser le fichier ou la doc.

**web/email/layout.ts**
- `renderEmailLayout` insère `preheaderText` brut dans le HTML ; les appelants actuels échappent bien (et les tests le vérifient), mais le docstring devrait expliciter cette responsabilité pour éviter un futur oubli (donnée habitant non échappée → injection HTML dans l'e-mail).

**web/email/brevo.ts**
- Échappement correct et testé, RAS. Rappel : `replyTo` expose l'e-mail personnel de l'invitant au destinataire — choix assumé et documenté, à garder en tête côté données personnelles.

**web/routes/messages.tsx**
- Après envoi réussi, la redirection `/messages?with=…` ne reconduit pas `postId` : le bandeau « À propos de » disparaît dès le premier message. Si c'est voulu (contexte utile seulement à l'ouverture de la conversation), OK ; sinon le conserver dans la redirection.

Architecture : la répartition common/par-page est cohérente avec les usages réels des classes (`.fil-page`, `.fil-list__empty`, `.street-status__progress` justifiés dans `common.css`), les imports `with { type: "css" }` sont en place partout, `deno.json` (`raw-imports`) et la doc AGENTS.md sont alignés. Pas de problème de sécurité ni de fuite de données détecté.

VERDICT: OK
