# Revue IA — 2026-08-12T07:20:30.328Z

- Modèle : moonshotai/kimi-k3
- Commit parent : ef5a0c2 (ce fichier fait partie du commit en cours de création, juste après ce parent)
- Fichiers revus : _doc/backlog.md, web/assets/common.css, web/routes/_middleware.ts, web/routes/_middleware_test.ts, web/routes/rejoindre.tsx, web/routes/rejoindre_test.ts
- Verdict : OK

---
## Revue

### `web/routes/_middleware.ts`
- **`img-src 'self'` à vérifier contre les QR codes** : le backlog indique que le kit papier/QR d'invitation est livré. Si les QR codes sont rendus en `<img src="data:...">`, cette CSP les cassera en prod (le commentaire dit avoir vérifié l'absence de styles/scripts inline, mais pas l'absence d'images `data:`). Si ce sont des SVG inline ou une route dédiée, aucun souci. À confirmer avant merge.
- **En-têtes ANSSI manquants faciles** : le commentaire cite ANSSI-PA-009 R13-R22, mais `X-Content-Type-Options: nosniff` n'est pas posé (anti MIME-sniffing, quasi systématique dans ces reco). `Strict-Transport-Security` aussi si le TLS est terminé par l'app (à ignorer si un proxy amont s'en charge).
- Détail dev : `connect-src 'self'` reste strict en dev ; les navigateurs récents matchent `ws:`/`wss:` same-origin avec `'self'`, mais si le HMR Vite casse un jour, c'est la piste à regarder en premier.

### `web/routes/rejoindre.tsx`
- Ordre de validation : un formulaire vide affiche désormais l'erreur d'âge plutôt que « champs manquants ». C'est documenté et testé, donc assumé — juste noter que l'erreur générique n'est plus atteignable que si la case est cochée, ce qui est cohérent.
- Bon point vie privée : contrôle déclaratif sans stocker de date de naissance, cohérent avec la minimisation des données.

### `web/assets/common.css`
- `.form-field--checkbox .lookup-card__label` : sélecteur qui couple deux espaces de nommage (`form-field` et `lookup-card`). Ça fonctionne ici (le libellé est bien dans une lookup-card), mais une classe de libellé générique éviterait ce couplage si le pattern case-à-cocher se répète hors lookup-card. Mineur.

### Tests
- Couverture correcte des deux fonctionnalités (en-têtes en mode strict hors Vite, case d'âge prioritaire, re-soumission de `ageConfirmed`). Rien à redire.

VERDICT: OK
