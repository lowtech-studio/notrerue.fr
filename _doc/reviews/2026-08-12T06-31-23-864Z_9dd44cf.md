# Revue IA — 2026-08-12T06:31:23.864Z

- Modèle : moonshotai/kimi-k3
- Commit parent : 9dd44cf (ce fichier fait partie du commit en cours de création, juste après ce parent)
- Fichiers revus : AGENTS.md, README.md, _doc/backlog.md, _doc/reviews/2026-08-11T22-00-30-762Z_9dd44cf.md, compose.yaml, web/assets/pages/fil.css, web/assets/pages/profil.css, web/assets/pages/recommandations.css, web/db/account.ts, web/db/account_test.ts, web/db/comments.ts, web/db/posts.ts, web/db/schema.ts, web/db/streets.ts, web/db/users.ts, web/db/users_test.ts, web/islands/PrintButton.tsx, web/routes/fil.tsx, web/routes/index.tsx, web/routes/index_test.ts, web/routes/messages.tsx, web/routes/messages_test.ts, web/routes/modifier.ts, web/routes/modifier_test.ts, web/routes/profil.tsx, web/routes/recommandations.tsx, web/routes/reponses.ts, web/routes/reponses_test.ts, web/routes/supprimer-compte.ts, web/routes/supprimer-compte_test.ts, web/routes/supprimer.ts, web/utils/validation.ts
- Verdict : OK

---
## Revue

Le point BLOQUANT de la revue précédente (`registerInhabitant` comptant les foyers soft-supprimés) est corrigé, avec test de régression dédié — bien. Les suggestions précédentes (numéro de foyer effacé, taps soft-supprimés, formulaire masqué si compte supprimé, `edit_error`/`reponse_error`) sont aussi traitées. Le reste est globalement sain : contrôles d'accès serveur présents, anonymisation unique et libérant l'e-mail, session invalidée trois fois (cookie + `token: null` + filtre `deletedAt`), nommage anglais clair, découpage routes/db respecté.

### AGENTS.md
- Le reformatage automatique a cassé la section « Structure du projet » : l'arborescence (non protégée par un bloc de code fence) est désormais re-wrapée en texte courant qui mélange les branches (`├── routes/ … │ ├── _app.tsx …`) — illisible même en source. À remettre dans un bloc ``` fence. Idem pour la liste « low tech » (`· Partage · Entraide…`) désormais inline.
- « l'application se trouve dans le dossier app » : le dossier réel est `web/` (pré-existant, mais la section est retouchée ici, autant corriger).

### web/db/account.ts
- `notificationType` (jsonb) n'est pas purgé à la suppression, contrairement à login/email/token. Contenu inconnu aujourd'hui, mais si des préférences personnelles y atterrissent un jour, prévoir de le mettre à `null` dans le même `update`.
- Remarque mineure : la séquence non transactionnelle est documentée et idempotente (OK), mais une inscription concurrente entre le soft-delete du foyer et son marquage peut compter un foyer moribond — cas rarissime, cohérent avec le choix assumé, juste à garder en tête.

### web/assets/pages/fil.css / recommandations.css
- Bonne correction d'accessibilité vs `display: none`. Nit résiduel : pour un lecteur d'écran, la case reste annoncée comme une « case à cocher » sans notion d'expansion ; un `aria-expanded` sur le déclencheur (ou un commentaire assumant le choix) serait un plus. Non bloquant.

### web/routes/index.tsx
- `?compte_supprime=1` affiche le bandeau à quiconque visite l'URL avec ce paramètre — inoffensif, pas de donnée exposée.

### web/routes/supprimer-compte.ts / profil.tsx
- RAS : POST protégé par session, confirmation en deux clics via `<details>` natif, cohérent avec le pattern CSRF/SameSite du reste de l'app.

VERDICT: OK
