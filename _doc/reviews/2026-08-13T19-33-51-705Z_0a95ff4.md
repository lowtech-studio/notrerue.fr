# Revue IA — 2026-08-13T19:33:51.705Z

- Modèle : moonshotai/kimi-k3
- Commit parent : 0a95ff4 (ce fichier fait partie du commit en cours de création, juste après ce parent)
- Fichiers revus : README.md, _doc/backlog.md, compose.yaml, web/.env.example, web/assets/common.css, web/assets/pages/fil.css, web/db/client.ts, web/db/schema.ts, web/main.ts, web/routes/fil.tsx, web/static_test.ts, web/utils/static_cache.ts, web/utils/static_cache_test.ts
- Verdict : OK

---
## Revue

### compose.yaml
- Les variables `SEED_ADMIN_EMAIL` / `SEED_ADMIN_PASSWORD` sont supprimées : vérifier qu'aucun script de seed (ex. `db/seed*.ts` ou une tâche deno) ne les lit encore avec une erreur si absentes — sinon `docker compose up` casse en dev. Si le seed admin a été supprimé du code en même temps (hors diff), ignorer.

### web/db/schema.ts
- Les index ajoutés sont pertinents et bien justifiés (préfixe gauche pour `tap_post_id_idx`, index partiel pour `message_unread_idx`). En revanche le diff ne contient **aucune migration** : si le projet passe par `drizzle-kit generate` + migrations versionnées, elles doivent être générées et appliquées, sinon ces index n'existeront jamais en base (ni en dev existant, ni en prod). À confirmer selon le workflow retenu (`push` au démarrage ou migrations).

### web/db/client.ts
- RAS : `Number(...) || 5` gère correctement les valeurs absentes/invalides, `idle_timeout` cohérent avec la cible Raspberry Pi.

### web/main.ts + web/utils/static_cache.ts
- L'ordre des middlewares est correct (posé avant `staticFiles()`, ajustement sur le chemin retour) et le raisonnement du commentaire est juste. La liste blanche de chemins cacheables est prudente (sw.js/manifest exclus). 
- Remarque mineure : la liste des icônes est en dur dans `isLongCacheablePath` — penser à la faire évoluer si de nouvelles icônes PWA sont ajoutées (le test `static_cache_test.ts` aidera à ne pas l'oublier).

### web/static_test.ts (ex `web/static/pwa_test.ts`)
- Bon déplacement : le fichier de test était auparavant servi publiquement par `staticFiles()` (code source des tests exposé). Le nouveau chemin `web/static` via `import.meta.dirname` est correct.

### web/assets/common.css
- Suppression de `.site-header__form` : vérifier qu'aucun markup n'utilise encore cette classe (le formulaire de déconnexion semble désormais sous `.account-menu__form`, donc probablement OK).

### web/routes/fil.tsx + web/assets/pages/fil.css
- RAS : markup échappé par Preact, classes BEM cohérentes avec le reste.

### README.md / .env.example / backlog.md
- RAS : pas de secret réel commité (valeurs d'exemple dev uniquement, clés vides dans `.env.example`).

VERDICT: OK
