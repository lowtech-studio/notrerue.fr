# Revue IA — 2026-08-16T13:42:59.830Z

- Modèle : moonshotai/kimi-k3
- Commit parent : 16c4577 (ce fichier fait partie du commit en cours de création, juste après ce parent)
- Fichiers revus : web/db/users.ts, web/db/users_test.ts, web/db/vouches.ts, web/email/brevo.ts, web/routes/index.tsx, web/routes/rejoindre.tsx
- Verdict : OK

---
## Revue

### web/db/users.ts
- `findStreetAmbassador` : la requête ne pose ni `ORDER BY` ni `LIMIT 1` — si une anomalie de données produisait deux ambassadeurs actifs sur une même rue (le verrou `FOR UPDATE` de `registerInhabitant` l'empêche en fonctionnement normal, mais rien ne le garantit au niveau schéma), la ligne retournée serait arbitraire. Suggestion : `orderBy(user.createdAt)` + `limit(1)` pour au moins rendre le choix déterministe (le premier inscrit), voire un index unique partiel `(house.streetId) WHERE is_ambassador AND deleted_at IS NULL` côté schéma. Non bloquant, mais le commentaire de la fonction affirme « le premier inscrit » : autant l'assurer.
- Le cas « ambassadeur supprimé ⇒ plus personne à notifier » est honnêtement documenté comme non géré (backlog) : OK, mais à bien tracer, car c'est désormais le point de défaillance unique de la chaîne de validation pour toute une rue.

### web/routes/rejoindre.tsx
- Le passage de `Promise.allSettled` à un `try/catch` unique est équivalent (notification tolérante à l'échec, loguée). La requête `findStreetAmbassador` elle-même reste hors du `try` (une erreur DB ferait échouer le POST après une inscription réussie) — comportement identique à l'ancien `listVerifiedNeighbors`, pas de régression, mais un englobement dans le try serait plus robuste.
- Noter le changement de périmètre produit : seuls les ambassadeurs reçoivent l'e-mail, alors que la page d'accueil propose le bouton de validation à *tous* les voisins vérifiés. Décision assumée dans les commentaires (retour utilisateur), rien à signaler côté code.

### web/db/vouches.ts, web/email/brevo.ts
- Docs mises à jour de façon cohérente avec le nouveau ciblage. Adjacent (hors diff mais même sujet) : le JSDoc de `sendStreetAwakeningEmail` dit toujours « Notification à l'ambassadeur » alors que l'appelant l'envoie à tous les inscrits — doc devenue fausse, à aligner.

### web/db/users_test.ts
- Bonne couverture (ambassadeur trouvé, autre rue exclue, compte supprimé, rue vide), nommage et helpers de nettoyage cohérents avec l'existant.

### web/routes/index.tsx
- Uniquement du re-wrap de texte, rien à signaler.

VERDICT: OK
