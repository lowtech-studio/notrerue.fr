# Revue IA — 2026-08-16T13:38:22.521Z

- Modèle : moonshotai/kimi-k3
- Commit parent : ced66b4 (ce fichier fait partie du commit en cours de création, juste après ce parent)
- Fichiers revus : web/assets/pages/fil.css, web/assets/pages/inviter.css, web/email/brevo.ts, web/email/brevo_test.ts, web/routes/fil.tsx, web/routes/index.tsx, web/routes/inviter.tsx, web/routes/inviter_test.ts
- Verdict : OK

---
## Revue

### web/routes/index.tsx
- « La rue s'allume à partir de **4 foyers** inscrits. » : seuil écrit en dur dans le texte alors que `STREET_AWAKENING_THRESHOLD` est déjà importé dans ce fichier et utilisé quelques lignes plus bas (`sur {STREET_AWAKENING_THRESHOLD}`). Si le seuil évolue, ce texte mentira — utiliser la constante ici aussi.

### web/routes/fil.tsx
- Le commentaire du `<h1>` contredit le code : il affirme « pas de "rue" ajouté ici », mais le JSX rend `Le fil de la rue "{streetName}"`. Comme `streetName` porte déjà le type de voie, on obtient « Le fil de la rue "Rue Jacoulet" » (doublon rue/Rue), et le `<title>` (« Le fil de {streetName} ») n'est plus aligné malgré le « même convention » du commentaire. Choisir une forme et corriger commentaire + code en conséquence.
- Guillemets droits `"..."` autour du nom de rue, alors que la page utilise les guillemets français ailleurs (« {search} » plus bas, badges, etc.) — harmoniser.

### web/assets/pages/fil.css
- Commentaire de `.compose-post` désynchronisé : il annonce « 40% à partir de 760px », la media query fixe `width: 60%`.
- `.compose-post-toggle` est la 3ᵉ copie quasi identique du bloc de masquage accessible dans ce même fichier (`.image-dropzone__input`, `.fil-post__edit-toggle`/`.fil-post__delete-toggle`). Cohérent avec la convention de duplication commentée du projet, mais une classe utilitaire commune limiterait les dérives futures — simple suggestion.

### web/routes/inviter.tsx + web/email/brevo.ts
- Bon point sécurité : échappement HTML avant interpolation, `\n` → `<br>` après échappement (pas d'injection), modération blocklist appliquée au mot personnel, et resoumission des valeurs en cas d'erreur — couvert par les tests.
- Remarque (non bloquante) : le mot personnel est du texte libre envoyé depuis l'expéditeur vérifié de la plateforme vers n'importe quelle adresse — vecteur de spam/phishing atténué seulement par le cooldown de 10 s et la blocklist. À garder à l'œil (p. ex. présence de liens dans le message), mais rien à changer dans ce diff.

VERDICT: OK
