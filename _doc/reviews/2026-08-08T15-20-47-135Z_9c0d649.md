# Revue IA — 2026-08-08T15:20:47.135Z

- Modèle : moonshotai/kimi-k3
- Commit parent : 9c0d649 (ce fichier fait partie du commit en cours de création, juste après ce parent)
- Fichiers revus : AGENTS.md, scripts/ai-review.ts
- Verdict : OK

---
## scripts/ai-review.ts

- Le commentaire `// ":!_doc/..." échoue : git lit le "_" comme un caractère magique` est douteux : `_` n'est pas un caractère magique dans les pathspecs git (la magie est introduite par `:` et `!`). Ce qui fait matcher `_doc/reviews/` à la racine, c'est le préfixe `**/`. Le pathspec fonctionne, mais le commentaire risque d'induire en erreur un futur lecteur — à reformuler ou supprimer.
- `saveReview` est appelé avant l'analyse du verdict, donc une revue BLOQUANT reste stagée et sera embarquée dans un commit ultérieur potentiellement sans rapport : c'est un vrai piège UX, mais il est explicitement documenté et assumé dans le docstring, et l'info horodatage/parent reste correcte. RAS de plus.
- Nommage : `saveReview`, `REVIEWS_DIR`, `parentSha`, `relPath` sont clairs et en anglais, cohérents avec le reste du script. `opts` est générique mais idiomatique, acceptable.
- Sécurité : le nom de fichier est construit uniquement à partir de `toISOString()` (caractères sûrs après remplacement) et d'un SHA hex court — pas d'injection de chemin possible. Le contenu Markdown provient du LLM mais n'est jamais exécuté, seulement écrit. Les erreurs sont fail-open avec warning, cohérent avec la philosophie du script.

## AGENTS.md

- RAS : la doc reflète fidèlement le comportement implémenté (sauvegarde dans `_doc/reviews/`, embarquée dans le commit).

VERDICT: OK
