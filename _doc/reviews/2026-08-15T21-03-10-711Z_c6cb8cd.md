# Revue IA — 2026-08-15T21:03:10.711Z

- Modèle : moonshotai/kimi-k3
- Commit parent : c6cb8cd (ce fichier fait partie du commit en cours de création, juste après ce parent)
- Fichiers revus : web/routes/fil.tsx, web/routes/fil_test.ts, web/routes/supprimer.ts, web/routes/supprimer_test.ts, web/utils/validation.ts
- Verdict : OK

---
## Revue

### web/routes/supprimer.ts
RAS — le contrôle d'accès (propriétaire uniquement) est toujours appliqué avant le soft delete, et le cas « demande d'un autre / postId invalide » redirige bien **sans** `deleted=1` (pas de fausse confirmation). La protection open redirect via `resolvePostBackPath` est préservée, `withQueryParam` ne réutilise que `pathname + search`.

### web/routes/fil.tsx
RAS — `postDeleted` est correctement initialisé dans les deux branches (GET et POST-erreur), le bandeau réutilise la classe existante `hero__confirmation`, texte statique (pas de XSS). Cohérent avec le pattern `postPublished`.

### web/utils/validation.ts
- **Nommage** : `withQueryParam(path, key, value)` a une signature générique mais un comportement désormais spécialisé (purge systématique des paramètres flash). Un nom comme `withFlashQueryParam` rendrait le contrat plus honnête à l'appel futur qui voudrait poser un paramètre non-flash sans perdre les flashs… ou l'inverse. La doc compense en partie, remarque légère.
- **Maintenabilité** : `FLASH_QUERY_PARAMS` duplique la connaissance des paramètres lus par `fil.tsx` GET (`published`, `deleted`, `edit_error`, `reponse_error`, `verif_error`). Si un nouveau flash est ajouté côté /fil sans mettre à jour cette liste, le bug de cumul de bandeaux réapparaît silencieusement. Le commentaire croisé existe côté validation.ts ; un rappel symétrique dans fil.tsx (ou une source unique partagée) sécuriserait l'évolution. Remarque légère également.

### web/routes/fil_test.ts / supprimer_test.ts
RAS — bonne couverture du cas régressif (`back` portant `?published=1` périmé) et du fallback open redirect avec `deleted=1`.

Aucun bug ni problème de sécurité identifié ; le changement de comportement de `withQueryParam` bénéficie aussi à /modifier et /reponses de façon cohérente.

VERDICT: OK
