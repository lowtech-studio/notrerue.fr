# Revue IA — 2026-08-10T15:25:02.885Z

- Modèle : moonshotai/kimi-k3
- Commit parent : 54ffd8d (ce fichier fait partie du commit en cours de création, juste après ce parent)
- Fichiers revus : _doc/backlog.md, scripts/ai-review.ts, web/assets/styles.css, web/db/posts.ts, web/db/posts_test.ts, web/db/schema.ts, web/deno.json, web/moderation/blocklist.ts, web/moderation/blocklist_test.ts, web/routes/index.tsx, web/routes/index_test.ts
- Verdict : OK

---
Revue du diff :

**web/routes/index.tsx**
- Nouveau endpoint POST sans jeton CSRF — comme les autres formulaires existants de l'app (logout, inscription, inviter), donc cohérent, mais ça repose entièrement sur un cookie de session `SameSite=Lax/Strict` : à confirmer une fois pour toutes, ce endpoint crée du contenu persistant.
- Troncature silencieuse du contenu à 240 caractères côté serveur (`.slice(0, MAX_POST_CONTENT_LENGTH)`) plutôt qu'une erreur : un message trop long est raccourci sans que l'utilisateur le sache (le `maxlength` HTML seul le prévient). Choix à assumer consciemment.
- Aucune limitation de débit : un habitant d'une rue allumée peut publier en boucle et spammer le futur fil. Acceptable pour un MVP, mais à tracer (le backlog prévoit déjà des limites pour les demandes urgentes).
- Nit nommage : la variable locale `postType` porte le même nom que l'enum `postType` exporté par `schema.ts`. Pas de conflit ici (l'enum n'est pas importé dans ce fichier), mais ça prête à confusion à la lecture croisée.
- `?published=1` affiche le bandeau de confirmation à quiconque ajoute le paramètre, sans publication réelle — anecdotique.

**web/db/schema.ts**
- Suppression de la colonne `post.title` : les migrations étant exclues de la revue, vérifier que la migration Drizzle de `drop column` est bien générée et appliquée. La table semble encore inutilisée en production (aucun code ne lisait `title`), donc pas de perte de données à craindre, mais un déploiement sans la migration casserait tout.

**web/moderation/blocklist.ts**
- Limite concrète du lookahead `(?![\p{L}\p{N}_])` : les formes fléchies non présentes dans la liste ne matchent pas (ex. "connards" si seul "connard" est listé, de même pour les accents non normalisés type "pede"). La doc du fichier assume déjà les limites de l'approche, mais un test sur les pluriels usuels serait un bon filet. Rien de bloquant.

**scripts/ai-review.ts**
- RAS : l'exclusion `:!**/.env` et le double chargement (racine puis `web/`, premier gagnant, sans écrasement) sont corrects et bien documentés. Les clés Brevo du `.env` racine sont chargées dans l'env du process mais jamais transmises (seul `OPENROUTER_API_KEY` part vers OpenRouter).

**web/db/posts.ts**, **web/assets/styles.css**, tests : RAS — séparation route/db/moderation propre, formulaire en POST natif sans island (bon choix de progressive enhancement), identifiants clairs.

VERDICT: OK
