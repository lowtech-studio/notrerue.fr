# Revue IA — 2026-08-15T11:08:52.675Z

- Modèle : moonshotai/kimi-k3
- Commit parent : e980854 (ce fichier fait partie du commit en cours de création, juste après ce parent)
- Fichiers revus : web/db/users.ts, web/email/brevo.ts, web/email/brevo_test.ts, web/routes/connexion.tsx, web/routes/index.tsx, web/routes/inviter.tsx, web/utils/image_test.ts
- Verdict : OK

---
## web/db/users.ts
- `clearLoginCodeSentAt` cible par `email` sans filtre `isNull(user.deletedAt)` (contrairement à `findUserByEmail` juste au-dessus) : si un compte soft-supprimé partage l'adresse, sa ligne est aussi modifiée (inoffensif mais imprécis). L'appelant dispose déjà de `outcome.user.id` — cibler par id serait plus exact et éviterait la re-recherche par e-mail.
- Course possible (bénigne) : deux `startLogin` concurrents suivis d'un `clear` peuvent effacer un throttle légitimement armé entre-temps. Vu le contexte (échec d'envoi), risque négligeable.

## web/routes/connexion.tsx
- Si `clearLoginCodeSentAt` jette (panne DB), le `catch` destiné à renvoyer un message gentil laisse repartir une 500 brute — exactement ce que le bloc cherchait à éviter. Envisager un try/catch (ou `.catch(() => {})` loggé) autour de ce nettoyage, qui est best-effort.
- La fin du commentaire (« l'utilisateur pouvait alors réessayer tout de suite ») se lit comme une conséquence du bug alors que c'est le comportement visé après le fix — formulation ambiguë, à clarifier (« peut désormais »).

## web/email/brevo.ts
- RAS sur le fond (doublon bien supprimé, couvert par le test de pied de page). Le commentaire inséré entre deux opérandes de concaténation est valide mais nuit un peu à la lecture ; le placer au-dessus du `const body` serait plus naturel.

VERDICT: OK
