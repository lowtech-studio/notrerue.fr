# Revue IA — 2026-08-16T20:38:54.191Z

- Modèle : moonshotai/kimi-k3
- Commit parent : 28de4a2 (ce fichier fait partie du commit en cours de création, juste après ce parent)
- Fichiers revus : web/components/icons.tsx, web/db/posts.ts, web/islands/PostComposerField.tsx, web/routes/fil.tsx, web/utils/csp.ts, web/utils/csp_test.ts
- Verdict : OK

---
## Revue

### web/db/posts.ts
- La refonte `count(*) over()` est correcte : la fenêtre est évaluée avant `LIMIT`/`OFFSET`, et le repli sur un `COUNT` dédié couvre bien les deux cas où la fenêtre est absente (page hors bornes, fil vide). Bornes et page retournée restent cohérentes avec l'ancien comportement dans tous les scénarios que j'ai rejoués.
- Remarque mineure de robustesse : le chemin nominal compte les lignes **jointes** (avec le `leftJoin postImage`), le repli compte les demandes **sans** cette jointure. Équivalents tant que `post ↔ postImage` reste 1-1 (le cas aujourd'hui), mais les deux comptages divergeraient silencieusement si la relation devenait 1-N un jour. Un mot dans le commentaire du repli le signalerait.
- Chemin nominal : `page` n'est plus explicitement bornée par `totalPages` — garanti implicitement (une page non vide est forcément ≤ `totalPages`), mais ça repose sur un invariant implicite ; un court commentaire l'assumerait.

### web/islands/PostComposerField.tsx
- Fusion propre, contrat identique aux deux îles supprimées (dégradation sans JS conservée, props sérialisables, pas de code serveur). Justification perf documentée.
- Suggestion de typage : `placeholders` est optionnel mais requis pour `variant="placeholder"`. Une union discriminée (`{ variant: "placeholder"; placeholders: Record<string, string> } | { variant: "dropzone" }`) rendrait le contrat explicite au lieu du `placeholders?.` défensif.
- Les classes CSS gardent les noms des anciens composants (`post-type-placeholder-field`, `image-dropzone__*`) — cohérent avec common.css et hors de portée du diff, mais le lien nom-classe ↔ composant est désormais indirect.

### web/routes/fil.tsx, web/components/icons.tsx, web/utils/csp.ts, web/utils/csp_test.ts
- Rien à signaler : usages du nouvel island corrects, références renommées dans les commentaires exactes.

VERDICT: OK
