# Revue IA — 2026-08-09T16:40:17.525Z

- Modèle : moonshotai/kimi-k3
- Commit parent : 86e3c86 (ce fichier fait partie du commit en cours de création, juste après ce parent)
- Fichiers revus : _doc/backlog.md, web/assets/styles.css, web/db/streets.ts, web/db/streets_test.ts, web/routes/index.tsx, web/routes/index_test.ts, web/routes/rejoindre.tsx, web/routes/rejoindre_test.ts
- Verdict : OK

---
## Revue

### web/db/streets.ts
- Commentaire trompeur : « Valeur reprise du prototype (`seuil`, défaut 8) » alors que la constante vaut `4`. Soit la valeur, soit le commentaire doit être corrigé — en l'état on ne sait pas lequel est juste.
- Nommage : `isAmbassadorSlot` est un peu obscur (« slot ») ; quelque chose comme `hasNoHouseholds` / `wouldBeAmbassador` serait plus parlant, d'autant que `rejoindre.tsx` expose le même concept sous le nom `willBeAmbassador`. Harmoniser les deux faciliterait la lecture croisée.

### web/routes/index.tsx
- Le parsing des query params (`cityId` / `city` / `street`, avec `MAX_CITY_LABEL_LENGTH`) est dupliqué quasiment à l'identique dans `rejoindre.tsx` — un helper partagé (ex. `parseAddressParams(url)`) éviterait la divergence future.
- `StreetStatusCard` : copie incohérente selon les branches — le nom de la rue apparaît dans certains titres (`La rue "..." dort encore.`) mais pas dans d'autres (« Il manque un foyer... », « Personne n'habite encore... »), et guillemets droits `"` au lieu des « » utilisés ailleurs. Cosmétique, à unifier.
- Remarque produit (non bloquante, c'est un item du backlog) : le nombre de foyers d'une rue devient public sans compte. C'est voulu, mais ça reste de la metadata de rue énumérable par n'importe qui — à garder en tête si le seuil/les compteurs évoluent vers plus de précision.

### web/routes/rejoindre.tsx
- `willBeAmbassador` est calculé au moment du GET (ou du POST en erreur) : entre l'affichage « Devenez le premier ambassadeur » et la soumission, un autre foyer peut s'être inscrit (TOCTOU). Le texte promet alors un rôle que l'inscription ne donnera pas — vérifier que `registerInhabitant` détermine bien le rôle côté serveur d'après l'état en base (le flag ne semble servir qu'à l'affichage, ce qui est correct, mais la promesse UI peut être fausse).
- `resolveWillBeAmbassador` est appelé dans le POST avant même la validation des champs (requête DB potentiellement inutile sur formulaire invalide) — anodin, mais déplaçable après le premier `if`.

### web/assets/styles.css
- `.street-status__title` code en dur `font-family: "Newsreader", Georgia, serif` au lieu de `var(--font-display)` — incohérent avec le reste du fichier, dont le commentaire d'en-tête précise justement que Newsreader n'est pas chargée.

### _doc/backlog.md
- L'URL courte mentionne encore `monquartier.fr/<ville>/<rue>` alors que le produit est notrerue.fr — coquille de domaine à corriger dans le doc.

Rien de bloquant : pas d'injection (Preact échappe les interpolations, requêtes paramétrées via Drizzle), props d'island sérialisables, tests couvrant bien les trois états du statut.

VERDICT: OK
