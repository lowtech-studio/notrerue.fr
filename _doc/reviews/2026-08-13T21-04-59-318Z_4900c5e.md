# Revue IA — 2026-08-13T21:04:59.318Z

- Modèle : moonshotai/kimi-k3
- Commit parent : 4900c5e (ce fichier fait partie du commit en cours de création, juste après ce parent)
- Fichiers revus : _doc/backlog.md, web/assets/pages/index.css, web/db/vouches.ts, web/db/vouches_test.ts, web/email/brevo.ts, web/email/brevo_test.ts, web/routes/index.tsx, web/routes/index_test.ts, web/routes/rejoindre.tsx
- Verdict : non détecté

---
## Revue

### web/routes/index.tsx + web/db/vouches.ts
- **Données personnelles** : `listVerifiedNeighbors` remonte `email`, et `HomeData.verifiedNeighbors` propage donc les e-mails des voisins vérifiés jusqu'aux props de la page d'accueil… alors que le rendu n'utilise que `id`/`login`. Certes la page est rendue côté serveur (pas de sérialisation vers le client aujourd'hui), mais c'est une exposition inutile d'une donnée sensible à un compte *non vérifié* — fragile si un îlot ou une sérialisation future venait à toucher `data`. Recommandé : ne sélectionner l'e-mail que pour l'usage notification (deux fonctions, ou un paramètre `includeEmail`), ou mapper côté handler vers `{ id, login }`.

### web/routes/rejoindre.tsx
- **Condition métier** : `if (!newInhabitant.isAmbassador)` teste un proxy de « pas encore vérifié ». Préférer `!newInhabitant.verifiedAt` — plus fidèle à l'intention et robuste si `registerInhabitant` évolue (ex. vérification automatique via invitation).
- **Gestion
