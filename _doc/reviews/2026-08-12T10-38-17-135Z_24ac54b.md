# Revue IA — 2026-08-12T10:38:17.135Z

- Modèle : moonshotai/kimi-k3
- Commit parent : 24ac54b (ce fichier fait partie du commit en cours de création, juste après ce parent)
- Fichiers revus : web/routes/inviter.tsx, web/routes/inviter_test.ts, web/routes/rejoindre.tsx, web/routes/rejoindre_test.ts
- Verdict : OK

---
## Revue

### web/routes/rejoindre.tsx
- **Sécurité (positif)** : le libellé ville n'est plus lu depuis le paramètre `city` (contrôlé par l'utilisateur, affiché tel quel dans le formulaire) mais reconstruit depuis la base via `findCityById`. Ça supprime un vecteur de lien forgé affichant un libellé arbitraire, et le `cityId` non résolu retombe proprement à `null`. Bon durcissement.
- **Cohérence** : le commentaire mentionne la page d'accueil comme source de liens `/rejoindre`, mais seul `/inviter` est modifié ici. Si l'accueil émet encore `?city=...`, ce paramètre devient du poids mort (inoffensif, simplement ignoré) — à aligner dans un commit séparé si ce n'est pas déjà fait.
- RAS sur la logique : parsing de `cityId` robuste (NaN, non-entier, négatif → `null`), pas de requête DB inutile quand `cityId` est absent, `willBeAmbassador` utilise bien l'identifiant validé.

### web/routes/inviter.tsx
- RAS. `URLSearchParams` gère correctement l'encodage des caractères spéciaux (`&`, `=`, accents) dans les noms de rue, et le `+` pour les espaces est bien décodé côté serveur. Le lien raccourci est cohérent avec l'objectif flyer/QR.

### web/routes/inviter_test.ts
- Le test reconstruit l'URL attendue avec le même `URLSearchParams` que l'implémentation : légèrement tautologique (ne détecterait pas une régression d'encodage), mais le test `/rejoindre` côté réception compense. Acceptable.

### web/routes/rejoindre_test.ts
- Bonne couverture des cas limites introduits par le changement : ancien lien avec `city` superflu (rétrocompatibilité) et `cityId` inexistant en base. RAS.

VERDICT: OK
